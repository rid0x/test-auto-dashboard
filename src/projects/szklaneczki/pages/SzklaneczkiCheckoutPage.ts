import { CheckoutPage } from '../../../core/pages/CheckoutPage';
import { healable, HealableLocator } from '../../../core/helpers/auto-healing';

export class SzklaneczkiCheckoutPage extends CheckoutPage {
  // Override selectors that differ from the core defaults
  // See src/core/pages/CheckoutPage.ts for available overrides
}
