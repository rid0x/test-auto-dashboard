import { CartPage } from '../../../core/pages/CartPage';
import { healable, HealableLocator } from '../../../core/helpers/auto-healing';

export class SzklaneczkiCartPage extends CartPage {
  // Override selectors that differ from the core defaults
  // See src/core/pages/CartPage.ts for available overrides
}
