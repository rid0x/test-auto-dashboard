import { SearchPage } from '../../../core/pages/SearchPage';
import { healable, HealableLocator } from '../../../core/helpers/auto-healing';

export class TestSearchPage extends SearchPage {
  // Override selectors that differ from the core defaults
  // See src/core/pages/SearchPage.ts for available overrides
}
