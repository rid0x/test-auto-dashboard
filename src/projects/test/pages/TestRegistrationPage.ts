import { RegistrationPage } from '../../../core/pages/RegistrationPage';
import { healable, HealableLocator } from '../../../core/helpers/auto-healing';

export class TestRegistrationPage extends RegistrationPage {
  // Override selectors that differ from the core defaults
  // See src/core/pages/RegistrationPage.ts for available overrides
}
