import { ProductPage } from '../../../core/pages/ProductPage';
import { healable, HealableLocator } from '../../../core/helpers/auto-healing';

export class TestProductPage extends ProductPage {
  // Override selectors that differ from the core defaults
  // See src/core/pages/ProductPage.ts for available overrides
}
