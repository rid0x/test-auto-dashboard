import { CategoryPage } from '../../../core/pages/CategoryPage';
import { healable, HealableLocator } from '../../../core/helpers/auto-healing';

export class TestCategoryPage extends CategoryPage {
  // Override selectors that differ from the core defaults
  // See src/core/pages/CategoryPage.ts for available overrides
}
